
import interpreter.Interpreter
import lexer.Lexer
import parser.Parser

class Cli(
    lexer: Lexer,
    parser: Parser,
    interpreter: Interpreter,
    linter: Linter,
    formatter: Formatter,
) {
    fun run(TODO: String) {
        TODO()
    }
}
